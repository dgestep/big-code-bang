<main>
    <br><br>
    <fieldset>
        <legend>Password Reset</legend>

        <p>Enter the email address you use to log in below.  An email will be sent with a reset code if an account
        exists with that address.</p>
        <br><br>
        <form method="post" [formGroup]="resetForm" (ngSubmit)="sendReset(resetForm.value)">
            <div class="row">
                <div class="col-lg-12">
                    <div class="input-group">
                        <input formControlName="emailAddress" type="text" class="form-control" placeholder="Email Address">
                        <span class="input-group-btn">
                            <button class="btn btn-default" type="submit">Send Reset</button>
                        </span>
                    </div>
                </div>
            </div>
        </form>
    </fieldset>
    <br>
    <res-messages></res-messages>
</main>