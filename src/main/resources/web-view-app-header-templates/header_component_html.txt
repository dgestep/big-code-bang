<nav class="navbar ln-navigation" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <span class="navbar-brand">
              <a [routerLink]="['/']">${companyNameLong}</a>
            </span>
        </div>

        <div id="navbar" class="navbar-collapse collapse">
            <!-- Not Logged-In -->
            <form *ngIf="loggedIn === false" method="post" class="navbar-form navbar-right" role="form">
                <div class="input-group">
                    <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                    <input type="text" class="form-control" name="username" placeholder="Email Address" #username required>
                </div>

                <div class="input-group">
                    <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                    <input type="password" class="form-control" name="password" placeholder="Password" #password required>
                </div>

                <button type="submit" class="btn btn-primary" (click)="onSubmit(username.value, password.value)">Login</button>
            </form>
            <!-- /Not Logged-In -->

            <!-- Logged-In -->
            <div class="pull-right user-profile">
                <div class="nav">
                    <div *ngIf="loggedIn" dropdown class="dropdown">
                        <a href="#" dropdownToggle class="dropdown-toggle header-link"><i class="glyphicon glyphicon-user"></i>  {{userEmail}} <b class="caret"></b></a>

                        <ul dropdownMenu role="menu" class="dropdown-menu dropdown-menu-right">
                            <li role="menuitem"><a (click)="navigateUserProfile()"> My Profile</a></li>
                            <li role="menuitem"><a (click)="createTab('Change Password', 'change-password')"> Change
                             Password</a></li>
                            <li class="divider"></li>
                            <li role="menuitem" *ngIf="adminUser"><a (click)="createTab('User Administration', 'user-admin')"> User Administration</a></li>
                            <li class="divider"></li>
                            <li role="menuitem"><a (click)="logout()"><i class="glyphicon glyphicon-log-out"></i> Logout</a></li>
                        </ul>
                    </div>
                </div>
            </div>
            <!-- /Logged-In -->
        </div>
    </div>
</nav>
