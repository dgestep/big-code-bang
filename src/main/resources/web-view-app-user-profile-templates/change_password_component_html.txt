<main>
    <h1>Change Password</h1>
    <hr>
    <form method="post" [formGroup]="passwordForm" (ngSubmit)="submitChange(passwordForm.value)">
        <div class="panel panel-default">
            <table class="table">
                <tr>
                    <td>Current Password:</td>
                    <td>
                        <input formControlName="currentPassword" name="currentPassword" type="password" class="form-control" placeholder="Current Password">
                    </td>
                </tr>
                <tr>
                    <td>New Password:</td>
                    <td>
                        <input formControlName="newPassword" name="newPassword" type="password" class="form-control" placeholder="New Password">
                    </td>
                </tr>
                <tr>
                    <td>Confirm Password:</td>
                    <td>
                        <input formControlName="confirmPassword" name="confirmPassword" type="password" class="form-control" placeholder="Confirm Password">
                    </td>
                </tr>
            </table>
        </div>
        <button type="submit" class="btn btn-primary" aria-label="Left Align" >
            <span class="glyphicon glyphicon-floppy-save" aria-hidden="true"></span> Change Password
        </button>
        <button type="button" class="btn btn-secondary" aria-label="Right Align" (click)="closeTab()">
            Cancel
        </button>
    </form>
    <res-messages></res-messages>
</main>