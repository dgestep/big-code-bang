import { ViewPage } from './app.po';

describe('view app', () => {
  let page: ViewPage;

  beforeEach(() => {
    page = new ViewPage();
  });

  it('should start', () => {
    page.navigateTo();
    expect true;
  });
});
