import {Component} from "@angular/core";
import {ResponseMessageHelper} from "../shared/response.message.helper";

@Component({
    selector: 'session-lost',
    templateUrl: 'app/session-lost/session.lost.component.html'
})
export class SessionLostComponent {
    constructor(private msgHelper : ResponseMessageHelper) {
        this.msgHelper.clearMessages();
    }
}