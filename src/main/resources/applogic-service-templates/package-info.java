/**
 * Base package, containing common domain implementations and helper classes, to be used by all service implementations.
 *
 * @author ${codeAuthor}.
 * @version 1.0
 * @since 1.0
 */
package ${topLevelDomain}.${companyName}.${productName}.model.service;